{
  "name": "@typoon/upload-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multipart chapter zip uploader shared by web SPA and browser extension. No build — consumers import .ts directly via vite/wxt aliases.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
