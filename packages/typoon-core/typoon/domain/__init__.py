"""Domain — pure data types. No dependencies outside stdlib + pydantic.

Import individual stage modules directly:
    from typoon.domain import scan          # module
    from typoon.domain.scan import Bubble   # type
"""
