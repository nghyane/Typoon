{
  "name": "typeset-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-wasm.mjs && node build.mjs",
    "deploy": "npm run build && wrangler deploy --no-bundle"
  },
  "dependencies": {
    "@jsquash/jpeg": "^1.5.0",
    "@jsquash/png": "^3.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260517.1",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.92.0"
  }
}
