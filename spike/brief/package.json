{
  "name": "brief-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "npm run build && wrangler deploy --no-bundle"
  },
  "dependencies": {
    "@jsquash/jpeg": "^1.5.0",
    "@jsquash/png":  "^3.1.0"
  },
  "devDependencies": {
    "esbuild":  "^0.28.0",
    "wrangler": "^4.92.0"
  }
}
