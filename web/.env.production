# Production build env — Cloudflare Pages picks this up at build time.
# Browsers running on https://mangalocal.com fetch the API cross-origin
# at https://api.mangalocal.com (which is the cloudflared tunnel pointed
# at the local FastAPI process).
VITE_API_URL=https://api.mangalocal.com
VITE_DISCORD_CLIENT_ID=927251094806098001
