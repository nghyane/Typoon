# GitHub Packages routing for @nghyane/bunle (used by web/).
#
# Required at the workspace root because bun resolves @nghyane/* against
# the registry from here when installing the web subworkspace.
@nghyane:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
