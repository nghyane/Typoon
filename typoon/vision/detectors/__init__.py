"""Detector backends — emit DetectionResult from a page image.

Each module here implements `TextDetector`. Selection happens in
`vision.factories._build_detector` based on `VisionPipelineSpec.detector`.
"""
